# Cross-cutting integration tests live here (per CLAUDE.md).
# Per-vehicle unit tests are under vehicle_dynamics/<vehicle>/core/tests/.
# This directory is intentionally empty until M4+ integration work begins.
